<DashboardPage>:
    name: "dashboard"
    
    BoxLayout:
        orientation: 'vertical'
        
        # Header
        BoxLayout:
            size_hint_y: None
            height: dp(60)
            padding: dp(10)
            spacing: dp(10)
            
            # Left side: Profile picture and info
            BoxLayout:
                orientation: 'horizontal'
                spacing: dp(10)
                
                # Profile picture placeholder (circle)
                Image:
                    source: ''  # Placeholder - replace with actual image path
                    size_hint: None, None
                    size: dp(40), dp(40)
                    canvas.before:
                        Color:
                            rgba: 0.8, 0.8, 0.8, 1  # Light gray background
                        Ellipse:
                            pos: self.pos
                            size: self.size
                    # Note: For actual circular image, you'd need a custom widget or use AsyncImage with mask
                
                # Name and age
                BoxLayout:
                    orientation: 'vertical'
                    Label:
                        text: "User Name"
                        font_size: sp(16)
                        halign: 'left'
                        valign: 'middle'
                        size_hint_y: None
                        height: self.texture_size[1]
                    Label:
                        text: "25 years old"
                        font_size: sp(12)
                        color: 0.5, 0.5, 0.5, 1
                        halign: 'left'
                        valign: 'middle'
                        size_hint_y: None
                        height: self.texture_size[1]
            
            # Right side: Edit button
            Button:
                text: "Edit"
                size_hint_x: None
                width: dp(80)
                on_press: app.root.current = "profile"
        
        # Main content area
        ScrollView:
            BoxLayout:
                orientation: 'vertical'
                padding: dp(20)
                spacing: dp(20)
                size_hint_y: None
                height: self.minimum_height
                
                # START MEAL button
                Button:
                    text: "START MEAL"
                    size_hint_y: None
                    height: dp(50)
                    size_hint_x: 0.99  # 90% of parent width
                    pos_hint: {'center_x': 0.5}
                    background_color: 0.2, 0.6, 1, 1  # Blue color
                    on_press: app.root.current = "camera"
                
                # TUTORIAL section
                Label:
                    text: "TUTORIAL"
                    font_size: sp(18)
                    size_hint_y: None
                    height: self.texture_size[1]
                    halign: 'left'
                    text_size: self.size
                
                # Tutorial placeholder
                BoxLayout:
                    size_hint_y: None
                    height: dp(150)
                    canvas.before:
                        Color:
                            rgba: 0.9, 0.9, 0.9, 1  # Light gray
                        RoundedRectangle:
                            pos: self.pos
                            size: self.size
                            radius: [dp(10),]
                    # Add tutorial content here later
                
                # PREVIOUS MEALS section
                Label:
                    text: "PREVIOUS MEALS"
                    font_size: sp(18)
                    size_hint_y: None
                    height: self.texture_size[1]
                    halign: 'left'
                    text_size: self.size
                
                # Container for dynamic meal items
                BoxLayout:
                    id: meals_list  # key
                    orientation: 'vertical'
                    spacing: dp(10)
                    size_hint_y: None
                    height: self.minimum_height